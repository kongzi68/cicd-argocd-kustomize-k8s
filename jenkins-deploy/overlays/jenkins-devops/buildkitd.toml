[worker.oci]
  enabled = false

[worker.containerd]
  enabled = true
  # namespace should be "k8s.io" for Kubernetes (including Rancher Desktop)
  namespace = "default"

[registry."iamIPaddr:11180"]
  http = true
  insecure = true

[registry."harbor.mydomain.com"]
  http = true
  insecure = true
